import './App.css';
// import useCount from './hooks/useCount';
function App() {
    //onCountChange(callback)
    return (
        <>
            <div>
                <h1>Count: 0</h1>
            </div>

            <button onClick={() => {}}>increment</button>

            <button onClick={() => {}}>decrement</button>

            <button onClick={() => {}}>incement by 3</button>

            <button onClick={() => {}}>incement debounced</button>
        </>
    );
}

export default App;
